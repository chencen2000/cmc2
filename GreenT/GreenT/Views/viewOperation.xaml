<UserControl x:Class="GreenT.Views.viewOperation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenT.Views"
             mc:Ignorable="d" 
             MinHeight="150" MinWidth="300">
    <Border Margin="4" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"
            BorderBrush="White" BorderThickness="1,1,1,1" CornerRadius="8,8,8,8">
        <Grid Margin="2" Background="#FFD8CDCD" >
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Label x:Name="labelInfo_task" Margin="10" Grid.Row="0"                        
                        FontSize="24"
                        FontWeight="Bold" 
                   Content="1"/>
            <Label Margin="10" Grid.Row="1"
                        Content="In Process"/>

            <Label Margin="10" Grid.Row="2"
                        Content=""/>
        </Grid>
        <Border.Effect>
            <DropShadowEffect Color="Gray" Opacity="0.5" />
        </Border.Effect>
    </Border>
</UserControl>
